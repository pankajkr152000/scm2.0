Complete Spring Boot Project | Smart Contact Manager

for initialising TailWind

    1. run command "npm init -y
            this will create a "package.json" in the project

    2. add " "type": "module"," in the "package.json" file after main

    3. run "npm install tailwindcss @tailwindcss/cli" 
            this will install TailWind packages 
            you can also check dependencies in "package.json" file to confirm

    4. run ""
            this will create output.css file at the provided location npx tailwindcss -i src/main/resources/static/css/input.css -o src/main/resources/static/css/output.css --watch
            and we need output.css file for styling





Thymeleaf Fragments -> for inserting commons html file into a html page like header or footer file which is commons
                       many html pages.

============================================================
:::::::::::::::::::::::    REQUEST    ::::::::::::::::::::::
============================================================
JSON (HTTP Request)
 ↓
Spring MVC
 ↓
@RequestBody + @Valid   ← validation (before controller logic)
 ↓
Controller
 ↓
Request DTO
 ↓
Mapper (DTO → Entity)
 ↓
Service (Interface)
 ↓
Service Implementation
 ↓
Business Logic
 ↓
Repository
 ↓
Database

============================================================
:::::::::::::::::::::::    RESPONSE    ::::::::::::::::::::::
============================================================
Database
 ↓
Repository
 ↓
Service
 ↓
Entity
 ↓
Mapper (Entity → Response DTO)
 ↓
Controller
 ↓
Response DTO
 ↓
JSON (HTTP Response)


============================================================
::::::::::::::::::::    Clear View    ::::::::::::::::::::::
============================================================
Client
  ↓
JSON
  ↓
Controller
  ↓
DTO  ↔ Mapper ↔ Entity
  ↓
Service (interface)
  ↓
ServiceImpl (logic)
  ↓
Repository
  ↓
Database




Transaction starts
 → lock row
 → increment value
 → save contact
 → commit


✅ Repository SHOULD
        Fetch / save data
❌ Repository SHOULD NOT
        Contain business logic



✅ Service SHOULD
        Handle business rules
        Handle transactions
        Decide what happens
❌ Service SHOULD NOT
        Parse JSON
        Handle HTTP objects



✅ Controller SHOULD
        Accept DTO
        Call service
        Return DTO
❌ Controller SHOULD NOT
        Generate IDs
        Call repositories
        Contain business logic



Golden rule (memorize this)
If a transactional method catches a RuntimeException,
the transaction is already dead.

======================================================================================
**************************************************************************************
======================================================================================

❌ What You Should NEVER Do
private User user;        // ❌
private static User user; // ❌

🧠 Final Recommendation (for your SCM project)

Since you’re building a learning + real-world Spring app:

👉 Use this combo:

@AuthenticationPrincipal User user → in controllers

SecurityContextHolder → inside services

@ModelAttribute → when user is needed everywhere

This keeps your app:

✅ Thread-safe

✅ Secure

✅ Production-grade

======================================================================================
**************************************************************************************
======================================================================================

to add backslash / in the starting of a file path url
th:src="@{'/' + *{contact.picture}}" 

Golden Thymeleaf rule (remember forever)
Syntax	Scope
${...}	Model attributes
*{...}	th:object
#{...}	Messages
@{...}	URLs


